# Copy to .env only if you still need local app secrets. Never commit .env.
# Infrastructure names live in infra/terraform.tfvars — not here.

# Key Vault / app secret value — set locally, do not commit.
# KV_SECRET_VALUE=
